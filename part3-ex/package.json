{
  "name": "part3-ex",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build:ui": "rm -rf build && cd ../part2-ex/ && rm -rf build && npm run build && cp -r build ../part3-ex/",
    "deploy": "git push heroku main",
    "deploy:full": "npm run build:ui && git add . && git commit -m buildui && npm run deploy",
    "dev": "nodemon index.js",
    "logs": "heroku logs --tail",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Tian Peng",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.1",
    "mongoose": "^6.3.8",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.16"
  }
}
